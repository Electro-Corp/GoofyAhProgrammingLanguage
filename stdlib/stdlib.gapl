func puts{
  // push 0 to the stack so when we add, it'll be the last two items on the stack, 0 and whatever var you wanted
  let tmp 0
  push tmp
  add
  out
}
func testCmp{
  let goofy 10
  let goofy1 10
  push goofy
  push goofy1
  // compare last two items on the stack
  cmp
  // put compare bit on the stack
  lmp
}